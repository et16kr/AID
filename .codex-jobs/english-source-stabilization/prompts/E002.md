# Job E002: Residual Korean Classification

## Goal

Classify every Hangul hit in English source files and fix only real untranslated English-source residue.

## Scope

- Source scan targets: `arch/Home` and `FAQE/Home`.
- Evidence target: `source-stabilization/residual-korean-classification.tsv`.
- Allowed source edits: English source lines that are truly untranslated Korean prose, comments, or procedure text.
- Preserve intentional Korean attachment filenames, encoded URL filenames, and sample data.

## Required Work

1. Run the worktree handoff check from the shared addendum.
2. Run `rg -n "[가-힣]" arch/Home FAQE/Home`.
3. Classify every hit as `intentional_attachment_filename`, `intentional_url_or_encoded_filename`, `intentional_sample_data`, `real_untranslated_text`, or `needs_recheck`.
4. For each `real_untranslated_text` hit, inspect nearby source context and translate to natural technical English only when safe.
5. Update `manifest.json` metadata for edited Markdown files if content changed.
6. Ensure no `real_untranslated_text` remains after safe fixes.
7. Run standard verification and repeat the Hangul scan.
8. Commit the result with message `Classify residual Korean in English sources`.

## Acceptance Criteria

- Every Hangul scan hit is represented in `source-stabilization/residual-korean-classification.tsv`.
- Intentional Korean filenames, URLs, and sample data are preserved.
- No unhandled `real_untranslated_text` remains.
- Any unresolved item is marked `needs_recheck` with a concrete blocker.
- Standard verification passes and project files are clean after the commit.
