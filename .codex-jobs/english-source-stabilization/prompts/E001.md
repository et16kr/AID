# Job E001: Phase 1 Completion Gate And Baseline

## Goal

Confirm Phase 1 ended with final decision `COMPLETE`, record the Phase 2 baseline, and create the `source-stabilization/` evidence structure.

## Scope

- Read `KO_EN_SEMANTIC_COVERAGE_REPORT.md` directly and use it as the Phase 1 source of truth.
- Do not rely on stale status values in `.codex-jobs/ko-en-semantic-coverage-audit/jobs.tsv` or `jobs.md`.
- Create or update only Phase 2 evidence files under `source-stabilization/`, plus `manifest.json` only if validation proves metadata is stale.
- Do not create `llm-reference/`.

## Required Work

1. Run the worktree handoff check from the shared addendum.
2. Confirm the final Phase 1 decision in `KO_EN_SEMANTIC_COVERAGE_REPORT.md` is `COMPLETE`.
3. Record current branch, HEAD, and baseline counts for `DOCK/Home`, `faq/Home`, `arch/Home`, and `FAQE/Home`.
4. Create `source-stabilization/README.md`.
5. Initialize the Phase 2 TSV files named in `workflow-requirements.md` with exact headers when they do not exist. If rerunning E001 over existing Phase 2 evidence, preserve later job rows unless intentionally restarting Phase 2 from a clean baseline.
6. Record in the README that Phase 2 must not run Phase 3 and must not create `llm-reference/`.
7. Run standard verification and review the final diff.
8. Commit the result with message `Add English source stabilization baseline`.

## Acceptance Criteria

- `source-stabilization/README.md` exists and explains Phase 2 purpose, evidence files, final decisions, and baseline counts.
- All planned Phase 2 TSV files exist with headers.
- Phase 1 final decision `COMPLETE` is explicitly recorded.
- Standard verification passes.
- Project files are clean after the focused commit.
