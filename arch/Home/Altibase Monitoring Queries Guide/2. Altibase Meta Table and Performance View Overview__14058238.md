---
title: "2. Altibase Meta Table and Performance View Overview"
page_id: "14058238"
space_key: "arch"
space_name: "Technical Documents(English)"
source_url: "https://docs.altibase.com/display/arch/2.+Altibase+Meta+Table+and+Performance+View+Overview"
updated_at: "2025-09-24T08:56:54.000+0900"
version: 6
ancestors: ["Home", "Altibase Monitoring Queries Guide"]
labels: []
---

# 2. Altibase Meta Table and Performance View Overview
Source: https://docs.altibase.com/display/arch/2.+Altibase+Meta+Table+and+Performance+View+Overview
Updated: 2025-09-24T08:56:54.000+0900

**- [Overview](#id-2.AltibaseMetaTableandPerformanceViewOverview-Overview) - [About ERD Notation](#id-2.AltibaseMetaTableandPerformanceViewOverview-AboutERDNotation) - [Note](#id-2.AltibaseMetaTableandPerformanceViewOverview-Note) - [Terms](#id-2.AltibaseMetaTableandPerformanceViewOverview-Terms)**

## **Overview**

---

For internal monitoring of Altibase, it is necessary to understand meta tables and performance views, which are called the data dictionary. In this document, the relationship between meta tables and performance views is expressed using an entity-relationship diagram (ERD) for user understanding and convenience. This section explains the prerequisite concepts.

### About ERD Notation

In consideration of the user's understanding and convenience, the performance view characteristics that do not have a base table are ignored, and the performance view is expressed as a table as shown below, and the key to be referenced when joining is express in FK form. To avoid confusion caused by this notation, the meta table has a yellow background and the performance view has a white background.

![image2016-7-13%209_28_9.png](https://docs.altibase.com/download/attachments/embedded-page/arch/2.%20Altibase%20Meta%20Table%20and%20Performance%20View%20Overview/image2016-7-13%209_28_9.png?api=v2)

In addition, in the above ERD, a column of the same attribute, such as the ID columns of the meta table and the META_ID column of the performance view, may have different name according to the meta table and the performance view. In this case, the same color is given to the column to indicate the same attribute.

### Note

It should be noted that Altibase meta table and performance view have the following characteristics.

1. Column of the same attribute may have different name according to meta table and performance views.
2. Depending on the Altibase server version, the column name of meta table and performance view can be changed or deleted.
3. Meta table and performance view may be added or deleted depending on the Altibase server version.

### Terms

This is a definition of some of the terms that may cause confusion prior to explanation.

- Session

  This refers to the user's access unit connected to the Altibase server. One user can have multiple sessions at the same time
- Statement

  As a term that means each SQL executed in a transaction, 'SQL statement', 'Query', 'Query statement,' often has the same meaning depending on the context. In this document, it is unified as 'query'.
- Memory DB GC (Garbage Collector) or GC

  When the user executes commit, the record before the change maintained by the MVCC (Multi Versioning Concurrency Control) technique should be deleted. Altibase operates separate threads to delete records before changes, which are called memory DB GC and disk DB GC respectively. However, since the MVCC method of the disk DB has changed since Altibase 5.3.3, the disk DB GC disappears, so only the memory DB GC exists, so it is called GC or Ager.
