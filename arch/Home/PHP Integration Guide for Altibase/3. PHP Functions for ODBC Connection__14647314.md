---
title: "3. PHP Functions for ODBC Connection"
page_id: "14647314"
space_key: "arch"
space_name: "Technical Documents(English)"
source_url: "https://docs.altibase.com/display/arch/3.+PHP+Functions+for+ODBC+Connection"
updated_at: "2021-02-18T17:34:29.000+0900"
version: 2
ancestors: ["Home", "PHP Integration Guide for Altibase"]
labels: []
---

# 3. PHP Functions for ODBC Connection
Source: https://docs.altibase.com/display/arch/3.+PHP+Functions+for+ODBC+Connection
Updated: 2021-02-18T17:34:29.000+0900

- [Sample Test](#id-3.PHPFunctionsforODBCConnection-SampleTest)

---

Altibase supports standard ODBC, so you can use these ODBC functions in PHP to integrate with the Altibase server. For detailed descriptions of the ODBC functions used in PHP, refer to the PHP official manual site below:

[http://php.morva.net/manual/kr/index.php](http://php.morva.net/manual/kr/index.php)

## **Sample Test**

```
<?php
// SYSTEM DSN, USER_ID, USER_PASSWORD
$conn = odbc_connect('TESTDSN', 'SYS', 'MANAGER');

if ($conn) {

    // direct-execution
    echo "now, i create table t1 (id integer, name char(20)<br>";
    odbc_exec ( $conn, "drop table t1" );
    odbc_exec ( $conn, "create table t1 (id integer, name char(20))" );

    // prepare-execution
    echo "now, i insert into t1 value (100, Lee)<br>";
    $stmt = odbc_prepare ( $conn, "insert into t1 values (?, ?)" );

    $Insert = array (100, "Lee");
    if (! odbc_execute ( $stmt, $Insert )) {
        echo ("error");
    }

    // single-selection
    $res = odbc_do ( $conn, "select id, name, sysdate from T1" );
    odbc_fetch_row ( $res );
    $ID = odbc_result ( $res, 1 );
    $NAME = odbc_result ( $res, 2 );
    $DATE = odbc_result ( $res, 3 );

    echo ("id = $ID , name = $NAME datetime = $DATE <br>");

    odbc_close ( $conn );
}
else
{
  echo "connection failed ...";
}
?>
```
